## 1. Move Shared Ebook Fixtures

- [x] 1.1 Create `test/fixtures/ebook-library/` directory at the monorepo root and move the 4 fixture files from `apps/api/test/ebook-library/` into it
- [x] 1.2 Update `apps/api/test/setup/test-env.ts` to resolve `EBOOK_LIBRARY_PATH` from the new path: `<monorepo-root>/test/fixtures/ebook-library/`
- [x] 1.3 Run `npm run test:e2e` from `apps/api` to confirm the API e2e suite still passes with the moved fixtures

## 2. Playwright Installation & Config

- [x] 2.1 Add `@playwright/test` as a dev dependency in `apps/web/package.json`
- [x] 2.2 Create `apps/web/playwright.config.ts` with two projects: `e2e` (Chromium, tests in `e2e/`) and `screenshots` (Chromium, tests in `e2e/screenshots/`); set `baseURL` from `PLAYWRIGHT_BASE_URL` (fallback `http://localhost:4173`); read `SCREENSHOTS_OUTPUT_DIR` (fallback `docs/screenshots/`) and pass it to screenshot tests via an env var
- [x] 2.3 Add `e2e` and `screenshots` scripts to `apps/web/package.json` (`playwright test` and `playwright test --project=screenshots` respectively)
- [x] 2.4 Create `apps/web/e2e/` and `apps/web/e2e/screenshots/` directories with a `.gitkeep`
- [x] 2.5 Add `apps/web/test-results/`, `apps/web/playwright-report/`, and `docs/screenshots/*.png` to `.gitignore`

## 3. Playwright Global Setup

- [x] 3.1 Create `apps/web/e2e/global-setup.ts` that starts the full stack (or waits for `PLAYWRIGHT_BASE_URL` to be reachable), sets `EBOOK_LIBRARY_PATH` to `test/fixtures/ebook-library/` relative to the monorepo root, and seeds a test user via the API `/setup` endpoint if not already created
- [x] 3.2 Wire `globalSetup` into `playwright.config.ts`

## 4. Core E2E Tests

- [x] 4.1 Create `apps/web/e2e/auth.spec.ts`: successful login redirects to `/`, invalid credentials show error alert, unauthenticated navigation to `/` redirects to `/login`
- [x] 4.2 Create `apps/web/e2e/setup.spec.ts`: fresh instance with `setupRequired: true` redirects to `/setup`, completing setup redirects to `/login`
- [x] 4.3 Create `apps/web/e2e/library.spec.ts`: dashboard shows book cards, All Books page lists imported books, search input filters results
- [x] 4.4 Create `apps/web/e2e/book-detail.spec.ts`: clicking a book card opens the modal, Escape closes it, Edit Metadata tab is reachable
- [x] 4.5 Create `apps/web/e2e/navigation.spec.ts`: sidebar links navigate to `/settings` and `/admin/settings`, logout clears token and redirects to `/login`

## 5. Screenshot Capture Tests

- [x] 5.1 Create `apps/web/e2e/screenshots/capture.spec.ts` that reads `process.env.SCREENSHOTS_OUTPUT_DIR` for the output path, authenticates once, then visits each key page/state and calls `page.screenshot({ path: '<outputDir>/<name>.png', fullPage: true })`; cover: `login.png`, `dashboard.png`, `all-books.png`, `book-detail.png`, `settings.png`
- [x] 5.2 Create `docs/screenshots/` directory and add a `README.md` explaining the images are generated by running `npm run screenshots` locally — never committed by CI
- [x] 5.3 Add `playwright-screenshots/` to `.gitignore` (CI-only ephemeral output)

## 6. CI Integration

- [x] 6.1 Add an `e2e` job to `.github/workflows/pr-checks.yml` with `continue-on-error: true`; the job should: checkout, set up Node 20, install deps, install Playwright browsers (`npx playwright install --with-deps chromium`), start the full stack via `docker compose up -d`, wait for the API to be healthy, run `npm run e2e --workspace=@litara/web`, and upload `playwright-report/` as a CI artifact on failure
- [x] 6.2 In the same CI job, after the e2e tests, run the screenshots project with `SCREENSHOTS_OUTPUT_DIR=playwright-screenshots/` and upload `playwright-screenshots/` as a separate named GitHub Actions artifact so reviewers can inspect the current UI state on every PR
- [x] 6.3 Verify the job appears in the PR checks panel as advisory (yellow warning on failure, not blocking merge)
